<div class="media">
  <div class="media-body">
    <small>
      
      <% if user_is_authorized_for_comment?(comment) %>
        | <%= link_to "Delete", comment, method: :delete %>
      <% end %>
    </small>
    <p><%= comment.body %></p>
  </div>
</div>
